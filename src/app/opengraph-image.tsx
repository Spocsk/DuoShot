import { ogImage, size, contentType } from "@/lib/og-image";

export { size, contentType };
export const alt = "DuoShot — ZIP App Store Connect iPhone Duo, sans alpha ni clone 2.3.3";

export default function Image() {
  return ogImage("fr");
}
